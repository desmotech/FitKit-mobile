/** Resolved theme tokens passed down to the profile presentational pieces. */
export interface ColorTokens {
  card: string;
  foreground: string;
  background: string;
  muted: string;
  mutedFg: string;
  secondary: string;
  secondaryFg: string;
  border: string;
}
